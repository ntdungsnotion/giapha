// ============================================================
// giapha · js/domains/media.js
// Vai trò  : Nghiệp vụ ảnh và tư liệu (không tự tải lên — việc đó của services)
// Lớp      : domains — HÀM THUẦN
// Phụ thuộc: utils/id
// Phiên bản: 0.1.0 · Cập nhật: 15/08/2026 12:16
// ============================================================

/** Gắn một ảnh đã tải lên vào một người hoặc một union. */
export function attachMedia(tree, subjectId, driveFileId, caption) { /* TODO */ }

export function detachMedia(tree, mediaId) { /* TODO */ }

/** Đặt một ảnh làm ảnh đại diện. */
export function setPortrait(tree, personId, mediaId) { /* TODO */ }

export function getMediaFor(tree, subjectId) { /* TODO */ }
